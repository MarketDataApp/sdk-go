# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Coverage files
*.out
coverage.html

# IDE/Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Dependency directories
vendor/

# Build output
dist/
bin/

# Environment / secrets
.env
.env.*
.envrc

# Claude Code (personal settings only; .claude/settings.json is shared)
.claude/settings.local.json
# Transient agent worktrees; a stray `git add -A` would otherwise commit them.
.claude/worktrees/

# CSV exports. The historical-exporter example defaults its output under the
# OS temp dir, but an explicit -outdir . still lands here.
*.csv
